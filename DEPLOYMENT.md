# 🚀 元衍智库网站部署指南

## 📋 项目概述

元衍智库是基于信息基因论（IGT v20）的跨学科研究与应用平台，使用VitePress构建的静态网站。

## ✅ 构建状态

网站已成功构建，所有页面均可正常访问。构建输出位于 `docs/.vitepress/dist/` 目录。

## 📦 部署准备

### 1. 环境要求
- Node.js 18+ 或兼容版本
- 推荐使用 npm 或 yarn 作为包管理器

### 2. 构建步骤

```bash
# 安装依赖
npm install

# 本地开发（可选）
npm run dev

# 构建生产版本
npm run build
```

## 📤 部署方式

### 1. GitHub Actions 自动部署（推荐）

#### Cloudflare Pages

1. **创建 Cloudflare Pages 项目**
   - 访问 [Cloudflare Pages](https://pages.cloudflare.com/) 并登录
   - 点击 "创建项目"，选择 "连接到 Git"
   - 选择 GitHub 仓库：`https://github.com/infogene-theory/-`
   - 配置项目名称为 `infometaverse`

2. **设置 GitHub Secrets**
   - 在 GitHub 仓库中，进入 "Settings" → "Secrets and variables" → "Actions"
   - 添加以下 Secrets：
     - `CLOUDFLARE_API_TOKEN`：您的 Cloudflare API 令牌（需要 Pages 编辑权限）
     - `CLOUDFLARE_ACCOUNT_ID`：您的 Cloudflare 账户 ID

3. **自动部署触发**
   - 当推送到 `main` 分支时，GitHub Actions 将自动构建并部署到 Cloudflare Pages
   - 也可以在 Actions 标签页手动触发部署

#### GitHub Pages
1. 在 GitHub 仓库中创建 `gh-pages` 分支
2. 将 `docs/.vitepress/dist/` 目录内容推送到该分支
3. 在仓库设置中启用 GitHub Pages，选择 `gh-pages` 分支

### 2. 手动部署

#### Vercel
1. 访问 [Vercel](https://vercel.com/) 并登录
2. 导入 GitHub 仓库
3. 配置构建命令：`npm run build`
4. 配置输出目录：`docs/.vitepress/dist`
5. 点击部署

#### Netlify
1. 访问 [Netlify](https://www.netlify.com/) 并登录
2. 导入 GitHub 仓库
3. 配置构建命令：`npm run build`
4. 配置发布目录：`docs/.vitepress/dist`
5. 点击部署

### 3. 自托管部署

将 `docs/.vitepress/dist/` 目录内容复制到您的 Web 服务器根目录：

```bash
# 使用 scp 复制到远程服务器
scp -r docs/.vitepress/dist/ user@server:/var/www/metagene/
```

## ⚙️ 配置说明

### 1. 主题配置

主题配置文件位于 `docs/.vitepress/config.js`，可自定义：
- 网站标题和描述
- 导航栏和侧边栏
- 主题颜色
- 数学公式引擎（当前使用 KaTeX）

### 2. 自定义样式

自定义 CSS 位于 `docs/.vitepress/theme/custom.css`，可修改：
- 主题颜色变量
- 排版样式
- 响应式设计
- 动画效果

### 3. Supabase 配置

数据存储和用户认证配置位于 `docs/.vitepress/supabase.js`，实际部署时需修改：
- `url`：您的 Supabase 项目 URL
- `anonKey`：您的 Supabase 匿名密钥

## 🔧 维护指南

### 1. 更新内容

- 理论内容：修改 `docs/theory/` 目录下的 Markdown 文件
- 应用工具：修改 `docs/applications/` 目录下的 Markdown 文件
- 案例库：修改 `docs/case-studies/` 目录下的 Markdown 文件
- 社区页面：修改 `docs/community/` 目录下的 Markdown 文件

### 2. 添加新页面

1. 在相应目录创建新的 Markdown 文件
2. 在 `docs/.vitepress/config.js` 中添加到导航栏或侧边栏
3. 重新构建网站

### 3. 常见问题

#### 构建失败
- 检查是否有死链接：使用 `npm run build` 命令会自动检查
- 检查 Markdown 语法错误
- 检查依赖是否安装完整

#### 数学公式不显示
- 确保已安装 `markdown-it-mathjax3` 或 `katex`
- 检查公式语法是否正确

## 📊 网站结构

```
docs/
├── index.md                 # 首页
├── theory/                  # 理论体系
│   └── volume1/            # 第一卷
├── applications/            # 应用工具箱
├── case-studies/           # 案例库
├── community/              # 社区
└── .vitepress/             # VitePress 配置
    ├── config.js           # 网站配置
    ├── theme/              # 主题配置
    ├── supabase.js         # Supabase 配置
    └── dist/               # 构建输出
```

## 📝 版本控制

- 网站版本：v1.0
- IGT 理论版本：v20
- 最后更新：2025年1月

## 🤝 贡献指南

欢迎贡献内容或代码，详见 `docs/community/contribute.md`。

## 🔒 域名和SSL证书配置

### Cloudflare Pages 域名配置（imeta.space）

由于 `imeta.space` 域名已经配置过，您需要将其关联到新的 Cloudflare Pages 项目：

1. **添加自定义域名到新项目**
   - 在 Cloudflare Pages 项目 `infometaverse` 中，进入 "自定义域" 选项卡
   - 点击 "设置自定义域"
   - 输入域名：`imeta.space`
   - 点击 "继续"

2. **更新 DNS 记录**（如果需要）
   - Cloudflare 将检查当前 DNS 记录
   - 如果域名已经在 Cloudflare 管理，系统会自动配置
   - 如果需要手动更新，Cloudflare 将提供新的 DNS 记录配置说明
   - 更新您的域名注册商或 DNS 提供商处的 CNAME 或 A 记录
   - 等待 DNS 记录生效（通常需要几分钟到几小时）

3. **验证 SSL/TLS 配置**
   - 在 Cloudflare 仪表板中，进入 "SSL/TLS" 选项卡
   - 确保 SSL/TLS 加密模式设置为 "完全" 或 "完全（严格）"
   - 验证 "始终使用 HTTPS" 选项已启用
   - 检查 SSL 证书状态是否为 "已颁发"

4. **移除旧项目的域名关联**（可选）
   - 如果 `imeta.space` 之前关联到其他 Cloudflare Pages 项目
   - 进入旧项目的 "自定义域" 选项卡
   - 点击域名旁边的 "管理" 按钮
   - 选择 "移除域名" 解除关联

### 其他托管服务的域名配置

#### Vercel
1. 在 Vercel 项目中，进入 "Settings" → "Domains"
2. 添加您的自定义域名
3. 按照提示配置 DNS 记录
4. SSL 证书将自动颁发和配置

#### Netlify
1. 在 Netlify 项目中，进入 "Site settings" → "Domain management"
2. 添加您的自定义域名
3. 按照提示配置 DNS 记录
4. SSL 证书将自动颁发和配置

#### GitHub Pages
1. 在 GitHub 仓库设置中，进入 "Pages" → "Custom domain"
2. 输入您的自定义域名
3. 配置 DNS 记录指向 GitHub Pages 服务器
4. 勾选 "Enforce HTTPS" 启用 SSL

## �� 许可证

本项目采用 MIT 许可证，详见 LICENSE 文件。
